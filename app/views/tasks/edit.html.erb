<h1>No.<%= @task.id %>のタスク編集画面</h1>

<%= form_for(@task) do |f| %>
  <%= f.label :content,"タスク"%>
  <%= f.text_field :content %>
  
  <%= f.submit "登録" %>
<% end %>

<%= link_to "タスク一覧に戻る", tasks_path %>